# Build log

## 2026-09-13
- Imported colleague's `statusline-command.py`, dropped Windows/PowerShell shims and the per-refresh debug dump.
- Added `install.sh` (copy + jq into settings.json with backup), `test.sh`, anonymised fixture, README.
- Installed locally; verified render from inside a git repo and with empty input.
